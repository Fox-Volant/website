import * as React from "react";

export const Head = ({ location, params, data, pageContext }) => (
    <>
        <title>Fox Volant | Home Page</title>
        <meta name="description" content="Fox Volant Home Page" />
    </>
);
